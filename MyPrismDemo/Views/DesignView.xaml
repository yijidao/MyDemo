<UserControl x:Class="MyPrismDemo.Views.DesignView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyPrismDemo.Views"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <ListBox x:Name="toolBox" Margin="5" ItemsSource="{Binding ToolBoxItems}" DisplayMemberPath="Name" Width="220" MouseMove="DragControl_ToolBoxMouseMove" MouseLeftButtonUp="CreateControl_GridOnMouseLeftButtonUp"></ListBox>
        <Border Grid.Column="1" Margin="5">
            <Grid x:Name="container" AllowDrop="True" Drop="Container_GridDrop" Background="Transparent"></Grid>
        </Border>
    </Grid>
</UserControl>
